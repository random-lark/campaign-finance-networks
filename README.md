# campaign-finance-networks

<img width="1019" height="972" alt="absorbing" src="https://github.com/user-attachments/assets/6fd0222b-d74e-49e3-9218-a884ab95c5fd" />

To run my data pipeline, run `data/download_data.sh` (this downloads and cleans the FEC data) then `data/process_data.sh` (this prepares the data for use by `igraph`). Run the analyses in `analysis/analysis.ipynb`. 
